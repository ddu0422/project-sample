pipeline {
    agent any
    environment {
        GITHUB_URL = 'https://github.com/ddu0422/project-sample'
    }

    stages {
        stage('Git Clone') {
            steps {
                git branch: 'main', credentialsId: 'GitHubCredential', url: "${GITHUB_URL}"
            }
        }
        stage('Build') {
            steps {
                withCredentials([aws(credentialsId: 'BuildCredential')]) {
                    sh 'aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 767397978317.dkr.ecr.ap-northeast-2.amazonaws.com'
                }
                withGradle {
                    sh './gradlew jib'
                }
            }
        }
    }
}
